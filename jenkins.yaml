
credentials:
  system:
    domainCredentials:
    - credentials:
      - usernamePassword:
          description: "Github Credentials"
          id: "githubCred"
          password: "{AQAAABAAAAAwUKg0yYoE6Cw6RYZ64y5Kces4qGlOORlMj0NTLP0hXrHlf4XAiuh2ZM1S3jAX8U/P3rRXawMa1v+1osph/BUomw==}"
          scope: GLOBAL
          username: "elemicatms"
jenkins:
  systemMessage: "Controlled by Configuration as Code"
  clouds:
    - docker:
        name: "docker"
        dockerApi:
          dockerHost:
            uri: "unix:///var/run/docker.sock"
        templates:
          - labelString: "docker-agent"
            dockerTemplateBase:
              # TODO: pin sha256 or versions when using in production
              image: "jenkins/agent"
              volumes:
                - hello:/hello
                - world:/world
              environmentsString: |
                hello=world
                foo=bar
            remoteFs: "/home/jenkins/agent"
            connector:
              attach:
                user: "jenkins"
            instanceCapStr: "10"
            retentionStrategy:
              idleMinutes: 1